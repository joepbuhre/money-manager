version: "2"
sql:
  - engine: "postgresql"
    queries: "schemas/query.sql"
    schema: "schemas/schema.sql"
    gen:
      go:
        package: "database"
        out: "database"
        sql_package: "pgx/v5"
